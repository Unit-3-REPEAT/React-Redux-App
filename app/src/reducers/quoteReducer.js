//Set up reducer

export const initialState = {
    quote:'',
    isFetching: '',
    error:''    
    }

export const quoteReducer = (state = initialState,action) => {

    switch(action.type){

        
        default:
            return state
    }

}    
